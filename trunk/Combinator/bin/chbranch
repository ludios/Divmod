#!/usr/bin/python

import sys
from combinator.branchmgr import theBranchManager

if 3 <= len(sys.argv) <= 4:
    theBranchManager.changeProjectBranch(*sys.argv[1:])
else:
    raise SystemExit("Usage: %s <project> <branch name> [trunk url]" % (sys.argv[0],))
